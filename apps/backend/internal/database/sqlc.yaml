version: "2"
sql:
  - engine: "sqlite"
    schema: "migrations"
    queries: "queries.sql"
    gen:
      go:
        package: "models"
        out: "models"
        emit_json_tags: true
        emit_empty_slices: true
        emit_interface: true
